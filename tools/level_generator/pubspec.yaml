name: level_generator
description: Offline Dart CLI for generating verified Skully Bones level packs.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.5.0
  puzzle_core: ^0.1.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
